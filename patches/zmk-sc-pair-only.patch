--- a/app/Kconfig
+++ b/app/Kconfig
@@ -145,7 +145,8 @@ menuconfig ZMK_BLE
     bool "BLE (HID over GATT)"
     select BT
     select BT_SMP
-    select BT_SMP_SC_PAIR_ONLY
+    # Don't force SC_PAIR_ONLY when HOGP module is enabled - external devices may use Legacy pairing
+    select BT_SMP_SC_PAIR_ONLY if !ZMK_HOGP
     select BT_SMP_APP_PAIRING_ACCEPT
     select BT_PERIPHERAL
     select BT_DIS
